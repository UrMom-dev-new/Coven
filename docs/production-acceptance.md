# Production Acceptance

Status values: Pass, Partial, Fail, Blocked.

| Requirement | Implementation | Evidence | Status |
| --- | --- | --- | --- |
| Preserve the approved visual reference in the repo. | Stored as `docs/design/coven-approved-reference.png`. | File exists in repository working tree. | Pass |
| Match approved top navigation: Coven brand, Sanctuary, Journal, Settings, compact view and workspace state. | `public/index.html`, `public/styles.css` and `public/src/app.js` provide the top bar, tabs, settings tray, compact control, demo badge and connection indicator. | JS syntax checks passed; authenticated browser visual smoke on port 8896 showed the expected topbar/settings markers. Windows visual acceptance still required. | Partial |
| Keep DEMO labeling only for demo data. | Demo badge is hidden unless `/api/status` reports demo mode; task rows display a Demo badge only when `task.mode === "demo"`. | Frontend syntax checks passed; `/api/status` smoke returned demo mode. | Pass |
| Maintain 58/21/21 Sanctuary/dialogue/journal composition. | CSS grid uses `58fr 21fr 21fr` with responsive collapse under narrower widths. | Authenticated browser screenshot showed the three-column composition with approved sanctuary art, dialogue and journal. Windows visual acceptance still required. | Partial |
| Render approved artwork direction, not generic dashboard placeholders. | Sanctuary, roster portraits, dialogue portrait and journal thumbnails use CSS crops from `public/assets/reference/coven-approved-reference.png`. | Browser visual smoke showed approved artwork pixels after replacing broken SVG wrappers. | Partial |
| Ensure Sanctuary map clicks are not swallowed by hotspot overlay. | `.hotspots` has `pointer-events: none`; `.hotspot` restores `pointer-events: auto`; hotspot labels are visually silent except keyboard focus so approved artwork labels remain dominant. | Browser visual smoke showed no duplicate labels over the approved art. Interactive Windows smoke still required. | Partial |
| Make Enter-to-talk prompt functional. | `public/src/game.js` tracks the nearest station and dispatches witch selection on Enter when not typing. | Node syntax check passed. | Pass |
| Save adventure hub presentation state. | Candle familiar map position is stored in `localStorage` and clamped on restore. | Node syntax check passed. | Partial |
| Persist Hermes session IDs. | Store schema now includes namespaced `runtimeSessions`; `HermesAdapter` reuses persisted IDs. | `test_runtime_sessions_are_namespaced_and_persisted` and `test_hermes_adapter_reuses_persisted_session` pass. | Pass |
| Provide real live task lifecycle: task, run, attempt, approval, cancel, retry, evidence and artifacts. | Live mode uses documented Hermes Runs API submission/status/stop/approval surfaces when `/v1/capabilities` advertises them; task records persist run/session IDs, attempts, idempotency keys, requested/served runtime, usage, approval, evidence and artifacts. | Mock contract tests cover run submission/reconciliation shape, validation-before-dispatch, retry linkage and terminal failure events. Live Hermes execution remains blocked by missing runtime/credentials. | Partial |
| Recover uncertain live submissions without duplicate local tasks. | Live task creation stores the run payload and payload hash before remote dispatch; same-key/same-payload submissions reuse the existing task and same-key/different-payload submissions are rejected. | Unit tests cover lost-response recovery, duplicate-safe local reuse and idempotency conflict rejection. Live Hermes idempotency replay remains unverified. | Partial |
| Keep task-list reads responsive during live runs. | Live run reconciliation is handled by a background `TaskReconciler`; task-list endpoints read cached state and status endpoints expose reconciler state. | Unit/store tests pass; live gateway polling requires Hermes. | Partial |
| Do not trust model-reported artifact existence. | Artifact claims are independently inspected only under configured workspace roots and annotated as `reported`, `inspected` or `validated`. | Unit tests cover no-root, valid-root, outside-root and malformed expected metadata cases. | Pass |
| Surface Office, Graph and GovDash integration state honestly. | `/api/integrations/status` reports workspace roots, Office bridge readiness, Graph cloud/token configuration, GovDash route status and tool schemas. Office operations fail closed when unavailable. | Unit tests cover disabled/blocked/default states, Graph cloud endpoint mapping, GovDash SharePoint root requirement and Office fail-closed behavior. Live Office/tenant/GovDash execution remains blocked. | Partial |
| Keep Ophelia terminal-failure cinematic guarded. | Event normalization requires failed, terminal and terminal-failure outcome before presentation; live failed attempts mark the no-auto-retry policy exhausted and de-duplicate event IDs. | Existing event tests and `test_live_task_failure_records_one_terminal_event` pass. | Pass |
| Provide real Windows/WebView2 voice. | The UI captures microphone audio as bounded WAV data and submits it to a local whisper.cpp service boundary. Transcripts are routed into explicit local commands or editable message/task drafts; no cloud speech fallback is allowed. | Unit tests cover command routing, WAV validation, cancellation and stale worker results. JS syntax checks passed. Runtime/model installation, microphone permission, installed voices and Windows/WebView2 speech quality remain unverified. | Partial |
| Boot desktop app surface without opening GUI. | `coven.desktop --self-test` starts the local service, authenticates, verifies approved-reference UI markers, seeded tasks, approved PNG and cache headers, then exits. | Source-tree desktop self-test passed on macOS with `/private/tmp/coven-desktop-self-test-final.log`; build script and Windows workflow run packaged `Coven.exe --self-test`. | Partial |
| Ship verified Windows portable bundle and installer. | PyInstaller/Inno/CI scaffolding exists; PyInstaller spec collects WebView backend modules; build script checks bundled web assets and approved reference image before running the executable self-test; CI compiles/uploads an unsigned Inno installer. | Local Windows build agent, clean install/upgrade/uninstall run and signing verification were unavailable in this workspace. | Blocked |
